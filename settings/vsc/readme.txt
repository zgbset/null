tasks.json和launch.json放在当前工作区的.vscode目录下。

settings.json分用户设置和工作区设置，这里的是用户设置，放在C:\Users\<username>\AppData\Roaming\Code\User下。工作区的设置在.vscode目录下。

keybindings.json放在上面的用户目录下，c.json放在用户目录的snippets下。

.clang-format和makefile放在当前工作区目录下。
